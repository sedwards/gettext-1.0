   make CFLAGS="-w"
   enforce_configure_first.sh
   bash link_unicase_dependencies.sh
   bash sync_and_link_unicode.sh
   bash header_fixes3.sh
   make CFLAGS="-w"



Autogenerated
./gettext-runtime/gnulib-lib/unictype.h
./gnulib-lib/unictype.h

Empty
/libasprintf/gnulib-lib/unictype.h

config.h - Add to the bottom of file after config
#ifndef _GL_ATTRIBUTE_ALWAYS_INLINE
# if defined __GNUC__ || defined __clang__
#  define _GL_ATTRIBUTE_ALWAYS_INLINE __attribute__ ((__always_inline__))
# else


#  define _GL_ATTRIBUTE_ALWAYS_INLINE
# endif
#endif

#ifndef _GL_INLINE_HEADER_BEGIN
# define _GL_INLINE_HEADER_BEGIN
# define _GL_INLINE_HEADER_END
#endif


# Fix whitespace errors in Makefiles
# 1. Back up your current Makefile state
cp gnulib-lib/Makefile gnulib-lib/Makefile.bak

# 2. Strip trailing spaces immediately following a backslash line continuer
sed -i '' 's/\\ \([ ]*\)$/\\/' gnulib-lib/Makefile

# 3. Clean up any carriage returns (\r) lingering in the Makefile text layout
sed -i '' 's/\r$//' gnulib-lib/Makefile

